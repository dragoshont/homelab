apiVersion: v1
kind: Service
metadata:
  name: homebridge
  namespace: default
spec:
  selector:
    app: homebridge
  ports:
    - name: http  # <-- hb ui
      protocol: TCP
      port: 80
      targetPort: 8581
    - name: secondary  # <-- Samsung OAuth
      protocol: TCP
      port: 8582
      targetPort: 8582